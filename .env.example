# Copy to .env and customize for your environment.
DISCORD_WEBHOOK_URLS=https://discord.com/api/webhooks/EXAMPLE/REPLACE_ME
# Comma or newline separated proxies. Leave blank to disable proxies.
PROXY_LIST=
# Optional flags to control startup behaviour.
DISABLE_WEBHOOK_VALIDATION=false
DISABLE_STARTUP_PING=false
